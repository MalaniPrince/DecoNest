package princetechlabs.deconest.ui.utils

import android.content.Context

class GetCount {

    fun GetCount(context: Context){

        var dataList = MasterDataUtils.MasterDataList(context)

    }
}